from typing import Any

def regex_pattern_match(pattern: str, value: Any) -> bool:
    """
    Matches the Given Pattern with the value.
    If it Does match return `True`.

    If it Does'nt match return `False`.

    Always return `False` for any value other than `str` type.
    """
